{
  "name": "ticking-away",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "zig build && vite build",
    "check": "tsc && prettier --check \"**/*.{ts,tsx,css,html,json,md,yaml,yml}\" && zig fmt --check build.zig bin lib",
    "check:fix": "prettier --write \"**/*.{ts,tsx,css,html,json,md,yaml,yml}\" && zig fmt build.zig bin lib",
    "ci": "npm run build && npm run check && npm test",
    "test": "zig build test"
  },
  "dependencies": {
    "@preact/signals": "^2.9.3",
    "preact": "^10.29.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^26.1.1",
    "prettier": "^3.9.4",
    "typescript": "^7.0.2",
    "vite": "^8.1.3"
  }
}
